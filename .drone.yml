kind: pipeline
name: default
type: docker

steps:
  - name: push-to-gar
    image: plugins/gcr
    pull: never
    settings:
      tag: latest
      repo: eoin-test
      registry_type: GAR
      location: us
      project_id: ci-play
      image_name: test
      json_key:
        from_secret: gcr_json_key
