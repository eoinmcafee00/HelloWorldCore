kind: pipeline
type: vm
name: default

pool:
  use: anka-build

steps:
  - name: check install
    commands:
      - sleep 100
      - hostname -f
      - ifconfig
      - touch /tmp/blaaaa
      - env
      - whoami
      - echo $PATH
      - echo $0
      - cat $0
